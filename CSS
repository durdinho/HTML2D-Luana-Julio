body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    font-size: 26px; /* Tamanho base da fonte */
}

header, nav, section, footer {
    margin: 20px;
}

header {
    text-align: center;
}

nav {
    text-align: center;
}

nav ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
}

nav li {
    display: inline;
    margin-right: 20px;
}

nav a {
    text-decoration: none;
    color: #333;
    font-weight: bold;
    font-size: 25px; /* Tamanho da fonte para links na barra de navegação */
}

section {
    margin-top: 20px;
}

.produto {
    cursor: pointer;
}

.produto img {
    max-width: 100%;
    height: auto;
}

#detalhes-produto {
    display: none;
    text-align: center;
    margin-top: 20px;
}

#produto-imagem {
    max-width: 100%;
    height: auto;
    margin-bottom: 10px;
}

#produto-nome {
    font-size: 24px; /* Tamanho da fonte para o nome do produto */
    margin-bottom: 10px;
}

#produto-preco {
    font-size: 20px; /* Tamanho da fonte para o preço do produto */
}

footer {
    text-align: center;
    margin-top: 50px;
    font-size: 14px; /* Tamanho da fonte no rodapé */
}
